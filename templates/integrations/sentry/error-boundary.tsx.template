// ============================================================================
// Sentry-backed React error boundary for Next.js App Router
// Generated by {{PROJECT_NAME}} harness-marketplace plugin v{{VERSION}}
//
// Drop this component in your root layout to capture client-side render errors.
// Example (app/layout.tsx):
//   import { AppErrorBoundary } from "@/integrations/sentry/error-boundary";
//   export default function RootLayout({ children }) {
//     return (
//       <html><body>
//         <AppErrorBoundary>{children}</AppErrorBoundary>
//       </body></html>
//     );
//   }
// ============================================================================

"use client";

import { ErrorBoundary } from "@sentry/nextjs";
import type { ReactNode } from "react";

type AppErrorBoundaryProps = {
  children: ReactNode;
  fallback?: ReactNode;
};

export function AppErrorBoundary({ children, fallback }: AppErrorBoundaryProps) {
  return (
    <ErrorBoundary
      fallback={
        fallback ?? (
          <DefaultFallback />
        )
      }
      showDialog={false}
    >
      {children}
    </ErrorBoundary>
  );
}

function DefaultFallback() {
  return (
    <div role="alert" style={{ padding: "2rem", textAlign: "center" }}>
      <h2>Something went wrong.</h2>
      <p>Our team has been notified. Please refresh the page to retry.</p>
      <button
        onClick={() => {
          if (typeof window !== "undefined") window.location.reload();
        }}
      >
        Reload
      </button>
    </div>
  );
}

// ═══ CUSTOM RULES BELOW (preserved on upgrade) ═══
// Customize DefaultFallback or add nested boundaries for specific routes.
