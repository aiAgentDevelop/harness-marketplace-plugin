#!/usr/bin/env bash
# ============================================================================
# Protected Files Guard — v{{VERSION}}
# Event: PreToolUse | Matcher: Edit,Write
# Generated by {{PROJECT_NAME}} harness-marketplace plugin
# ============================================================================
set -euo pipefail

# ═══ GENERATED RULES (overwritten on upgrade) ═══

FILE_PATH="${1:-}"

if [[ -z "$FILE_PATH" ]]; then
  echo "protected-files: no file path provided"
  exit 0
fi

# Normalize path separators for cross-platform compatibility
FILE_PATH="${FILE_PATH//\\//}"

# Built-in protected patterns (always enforced)
BUILTIN_PATTERNS=(
  '.env'
  '.env.*'
  '.env.local'
  '.env.production'
  '*.lock'
  'package-lock.json'
  'pnpm-lock.yaml'
  'yarn.lock'
  'bun.lockb'
)

# Project-configured protected patterns
CONFIG_PATTERNS=(
  {{PROTECTED_FILES}}
)

check_pattern() {
  local pattern="$1"
  local filepath="$2"
  local basename
  basename="$(basename "$filepath")"

  # Check against basename using glob matching
  # shellcheck disable=SC2254
  case "$basename" in
    $pattern) return 0 ;;
  esac

  # Check against full path using glob matching
  # shellcheck disable=SC2254
  case "$filepath" in
    *$pattern*) return 0 ;;
  esac

  return 1
}

# Check built-in patterns
for pattern in "${BUILTIN_PATTERNS[@]}"; do
  if check_pattern "$pattern" "$FILE_PATH"; then
    echo "[PROTECTED] File '$FILE_PATH' matches protected pattern '$pattern'."
    echo "This file should not be modified by automated tools."
    echo "If you must edit this file, do so manually and review carefully."
    exit 1
  fi
done

# Check project-configured patterns
for pattern in "${CONFIG_PATTERNS[@]}"; do
  [[ -z "$pattern" ]] && continue
  if check_pattern "$pattern" "$FILE_PATH"; then
    echo "[PROTECTED] File '$FILE_PATH' matches protected pattern '$pattern'."
    echo "This file is protected by project configuration."
    exit 1
  fi
done

# ═══ CUSTOM RULES BELOW (preserved on upgrade) ═══

# Add your own protected file patterns here.
# They will be preserved when the plugin is upgraded.
#
# Example:
#   case "$(basename "$FILE_PATH")" in
#     'my-critical-config.yaml')
#       echo "[PROTECTED] Custom rule: my-critical-config.yaml is protected"
#       exit 1
#       ;;
#   esac

exit 0
